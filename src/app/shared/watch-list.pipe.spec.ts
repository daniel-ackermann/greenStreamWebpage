import { WatchListPipe } from './watch-list.pipe';

describe('WatchListPipe', () => {
  it('create an instance', () => {
    const pipe = new WatchListPipe();
    expect(pipe).toBeTruthy();
  });
});
